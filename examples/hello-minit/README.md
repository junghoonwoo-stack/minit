# Hello Minit

A zero-dependency demo app.

From this directory:

```bash
python -m http.server 8000
```

Then, in another terminal from the Minit repository:

```bash
minit run --port 8000
```

Open the generated public URL on another device. If you see **Hello from Minit**, the full local-to-live path is working.
